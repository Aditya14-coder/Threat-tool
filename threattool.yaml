services:
  - type: web
    name: threatlens
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: VT_API_KEY
        sync: false